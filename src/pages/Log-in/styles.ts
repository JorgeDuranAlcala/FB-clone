import { makeStyles, Theme } from "@material-ui/core";

export default makeStyles((theme: Theme) => ({
    root: {
        display: 'grid', 
        placeItems: 'center',
        width: '100%',
        minHeight: '100vh'
    }
}))